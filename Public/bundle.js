var app = (function (internal) {
  'use strict';

  /* front-end/App.svelte generated by Svelte v3.44.3 */

  function create_fragment(ctx) {
  	let main;
  	let h1;

  	return {
  		c() {
  			main = internal.element("main");
  			h1 = internal.element("h1");
  			h1.textContent = `Hey there ${name}`;
  		},
  		m(target, anchor) {
  			internal.insert(target, main, anchor);
  			internal.append(main, h1);
  		},
  		p: internal.noop,
  		i: internal.noop,
  		o: internal.noop,
  		d(detaching) {
  			if (detaching) internal.detach(main);
  		}
  	};
  }

  let name = "Ben";

  class App extends internal.SvelteComponent {
  	constructor(options) {
  		super();
  		internal.init(this, options, null, create_fragment, internal.safe_not_equal, {});
  	}
  }

  const app = new App({
      target: document.body,
      props: {}
  });

  return app;

})(internal);
//# sourceMappingURL=bundle.js.map
